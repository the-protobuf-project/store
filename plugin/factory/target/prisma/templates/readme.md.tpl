<!-- Code generated by protoc-gen-store. DO NOT EDIT. -->

# {{.Database}} — Prisma schema

Generated from Protobuf by [protoc-gen-store](https://github.com/the-protobuf-project/store).
The source of truth is the `.proto` files — regenerate rather than editing these.

## Layout

```
{{.Database}}/
├── {{.Database}}.config.ts   Prisma 7 config (datasource URL via env)
├── schema.prisma             datasource + generator blocks
├── <schema>/<domain>.{{.ProviderExt}}.prisma   models & enums, one file per source proto
├── package.json
├── tsconfig.json
├── .env                      pre-populated {{.EnvVar}} (git-ignored)
└── .env.example
```

## Setup

```bash
npm install                 # .env ships pre-populated; edit {{.EnvVar}} if needed
npm run prisma:generate     # generate the typed client
npm run prisma:migrate      # create & apply a dev migration
```

All scripts target `{{.Database}}.config.ts` via `--config`.
