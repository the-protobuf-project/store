// Code generated by protoc-gen-store. DO NOT EDIT.
// versions:
// 	protoc-gen-store (unknown)
// 	protoc (unknown)
// source: wkt.proto
//
// database: wkt_db
// schemas:  kitchen
//
// annotations: protokit.v1 (unknown), store.v1 (unknown)
//
// store — https://github.com/the-protobuf-project/store

/// Sink is one table holding every interesting type mapping.
model Sink {
  /// Unique identifier for the record.
  id String @id @default(ulid()) @map("id")

  /// name: IDENTIFIER → PRIMARY KEY.
  name String @unique @map("name")

  /// bool → BOOLEAN.
  flag Boolean? @map("flag")

  /// int32 → INTEGER.
  count32 Int? @map("count32")

  /// int64 → BIGINT.
  count64 BigInt? @map("count64")

  /// float → REAL.
  ratio Float? @map("ratio")

  /// double → DOUBLE PRECISION.
  precise Float? @map("precise")

  /// bytes → BYTEA.
  blob Bytes? @map("blob")

  /// Timestamp → TIMESTAMPTZ.
  eventTime DateTime? @wkt_db.Timestamptz(6) @map("event_time")

  /// Duration → INTERVAL.
  window String? @map("window")

  /// FieldMask → TEXT.
  mask String? @map("mask")

  /// Int64 wrapper → BIGINT.
  maybeCount BigInt? @map("maybe_count")

  /// String wrapper → VARCHAR(255).
  maybeLabel String? @map("maybe_label")

  /// repeated string → VARCHAR(255)[].
  tags String[] @map("tags")

  /// repeated int32 → INTEGER[].
  scores Int[] @map("scores")

  /// map → JSONB.
  attributes Json? @map("attributes")

  /// Maps the model to the "sinks" table in the database.
  @@map("sinks")
  /// Maps the model to the "kitchen" schema in the database.
  @@schema("kitchen")
}
