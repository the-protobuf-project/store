// Code generated by protoc-gen-store. DO NOT EDIT.
// versions:
// 	protoc-gen-store (unknown)
// 	protoc (unknown)
// source: oneof.proto
//
// database: v1
// schemas:  oneof_v1
//
// annotations: entity.v1 (unknown), store.v1 (unknown)
// engine:      protokit (unknown)
//
// store — https://github.com/the-protobuf-project/store

/// Discriminator for the input oneof of Audio.
enum AudioInputCase {
  /// The audio_data member is set.
  AUDIO_DATA

  /// The upload_path member is set.
  UPLOAD_PATH

  /// The live_pipeline_file_path member is set.
  LIVE_PIPELINE_FILE_PATH

  /// Maps the enum to the "audio_input_case" type in the database.
  @@map("audio_input_case")
  /// Maps the enum to the "oneof_v1" schema in the database.
  @@schema("oneof_v1")
}

/// Discriminator for the end_form oneof of Booking.
enum BookingEndFormCase {
  /// The end member is set.
  END

  /// The duration member is set.
  DURATION

  /// Maps the enum to the "booking_end_form_case" type in the database.
  @@map("booking_end_form_case")
  /// Maps the enum to the "oneof_v1" schema in the database.
  @@schema("oneof_v1")
}

/// Audio exercises oneof integrity: the `input` oneof flattens to independent nullable columns, and store adds a generated input_case discriminator enum recording which member is set so the lost exclusivity invariant is observable.
model Audio {
  /// Unique identifier for the record.
  id String @id @default(ulid()) @map("id")

  /// Resource name; the AIP identifier.
  name String @unique @map("name")

  /// Inline audio bytes.
  audioData Bytes? @map("audio_data")

  /// Path to a previously uploaded file.
  uploadPath String? @map("upload_path")

  /// Path to a file produced by a live pipeline.
  livePipelineFilePath String? @map("live_pipeline_file_path")

  /// sample_rate is a plain scalar outside the oneof.
  sampleRate Int? @map("sample_rate")

  /// Discriminator: which input oneof member is set (null = none).
  inputCase AudioInputCase? @map("input_case")

  /// Maps the model to the "audios" table in the database.
  @@map("audios")
  /// Maps the model to the "oneof_v1" schema in the database.
  @@schema("oneof_v1")
}

/// Booking pins the RFC 7953 shape: well-known types as oneof members. These have no struct field of their own — protoc-gen-go puts each behind a wrapper on the oneof's interface field — so the converters must emit `out.EndForm = &Booking_Duration{Duration: …}`, not `out.Duration = …`. The enum arm is here for the same reason: it is a value type, so it guards on the zero rather than on nil. Without converters emitted for this message the flat form compiles here and fails only in a consumer's tree.
model Booking {
  /// Unique identifier for the record.
  id String @id @default(ulid()) @map("id")

  /// Resource name; the AIP identifier.
  name String @unique @map("name")

  /// Optional column mapped to "end".
  end DateTime? @v1.Timestamptz(6) @map("end")

  /// Optional column mapped to "duration".
  duration String? @map("duration")

  /// Discriminator: which end_form oneof member is set (null = none).
  endFormCase BookingEndFormCase? @map("end_form_case")

  /// Maps the model to the "bookings" table in the database.
  @@map("bookings")
  /// Maps the model to the "oneof_v1" schema in the database.
  @@schema("oneof_v1")
}
