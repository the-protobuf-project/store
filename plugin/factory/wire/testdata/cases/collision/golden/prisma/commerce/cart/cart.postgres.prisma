// Code generated by protoc-gen-store. DO NOT EDIT.
// versions:
// 	protoc-gen-store (unknown)
// 	protoc (unknown)
// source: cart.proto
//
// database: commerce
// schemas:  shop_cart_v1
//
// annotations: protokit.v1 (unknown), store.v1 (unknown)
//
// store — https://github.com/the-protobuf-project/store

/// Status enumerates cart payment states.
enum CartStatus {
  /// Represents the PENDING value.
  PENDING

  /// Represents the PAID value.
  PAID

  /// Maps the enum to the "cart_status" type in the database.
  @@map("cart_status")
  /// Maps the enum to the "shop_cart_v1" schema in the database.
  @@schema("shop_cart_v1")
}

/// Money is a cart-side resource. Its simple name "Money" collides with the order-side Money once both packages merge into the "commerce" database (see store.yaml). Prisma qualifies the colliding model names (its models share one global namespace), while the schema-namespaced targets — GORM (one package per schema), SQL, and CSV — keep the bare "Money", since the schema already disambiguates them.
model CartMoney {
  /// Unique identifier for the record.
  id String @id @default(ulid()) @map("id")

  /// Resource name; the AIP identifier.
  name String @unique @map("name")

  /// Amount in minor units.
  amount BigInt @map("amount")

  /// Status is a cart-side enum whose simple name collides with order.Status.
  status CartStatus @default(PENDING) @map("status")

  /// Maps the model to the "moneys" table in the database.
  @@map("moneys")
  /// Maps the model to the "shop_cart_v1" schema in the database.
  @@schema("shop_cart_v1")
}
