// Code generated by protoc-gen-store. DO NOT EDIT.
// versions:
// 	protoc-gen-store (unknown)
// 	protoc (unknown)
// source: order.proto
//
// database: commerce
// schemas:  shop_order_v1
//
// annotations: entity.v1 (unknown), store.v1 (unknown)
// engine:      protokit (unknown)
//
// store — https://github.com/the-protobuf-project/store

/// Status enumerates order fulfillment states.
enum OrderStatus {
  /// Represents the SHIPPED value.
  SHIPPED

  /// Represents the DELIVERED value.
  DELIVERED

  /// Maps the enum to the "order_status" type in the database.
  @@map("order_status")
  /// Maps the enum to the "shop_order_v1" schema in the database.
  @@schema("shop_order_v1")
}

/// Money is the order-side resource sharing the simple name "Money" with the cart-side one. See cart.proto for how each target renders the collision.
model OrderMoney {
  /// Unique identifier for the record.
  id String @id @default(ulid()) @map("id")

  /// Resource name; the AIP identifier.
  name String @unique @map("name")

  /// Amount in minor units.
  amount BigInt @map("amount")

  /// Status is an order-side enum whose simple name collides with cart.Status.
  status OrderStatus @default(SHIPPED) @map("status")

  /// Maps the model to the "moneys" table in the database.
  @@map("moneys")
  /// Maps the model to the "shop_order_v1" schema in the database.
  @@schema("shop_order_v1")
}
