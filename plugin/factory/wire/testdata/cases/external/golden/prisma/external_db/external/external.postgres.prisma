// Code generated by protoc-gen-store. DO NOT EDIT.
// versions:
// 	protoc-gen-store (unknown)
// 	protoc (unknown)
// source: external.proto
//
// database: external_db
// schemas:  external_v1
//
// annotations: protokit.v1 (unknown), store.v1 (unknown)
//
// store — https://github.com/the-protobuf-project/store

/// Product owns a price expressed as an imported Money value type.
model Product {
  /// Unique identifier for the record.
  id String @id @default(ulid()) @map("id")

  /// Resource name; the AIP identifier.
  name String @unique @map("name")

  /// Human-readable product title.
  title String @map("title")

  /// Foreign key to Money.
  priceID String @map("price_id")

  /// Relation to Money via price_id.
  price Money @relation(fields: [priceID], references: [id], onDelete: Cascade)

  /// Composite index on [price_id].
  @@index([priceID])
  /// Maps the model to the "products" table in the database.
  @@map("products")
  /// Maps the model to the "external_v1" schema in the database.
  @@schema("external_v1")
}
