// Code generated by protoc-gen-store. DO NOT EDIT.
// versions:
// 	protoc-gen-store (unknown)
// 	protoc (unknown)
// source: telemetryv1.proto
//
// database: weather_db
// schema:   weather
//
// annotations: entity.v1 (unknown), store.v1 (unknown)
// engine:      protokit (unknown)
// runtime:     gorm.io/gorm
//
// store — https://github.com/the-protobuf-project/store

package weather

import (
	"context"

	"example.com/test/gen/gormx"
	"gorm.io/gorm"
)

// CalibrationStoreIface is the full data-access surface of Calibration: everything
// CalibrationStore does, as an interface.
//
// It exists so a decorator — caching, tracing, retries, a test double — can be
// written in its own package against this contract, without that package needing
// to edit anything in this tree. Embed it, override the methods you care about,
// and delegate the rest:
//
//	type cachedCalibrationStore struct {
//		CalibrationStoreIface
//		cache Cache
//	}
//
// Construction is deliberately absent. NewCalibrationStore returns the concrete
// *CalibrationStore, and WithTelemetry is a chainable setter that also returns it, so
// a caller keeps the full type at the point of wiring and narrows to this
// interface at the point of use — which is the direction that stays useful when
// a decorator is added later.
type CalibrationStoreIface interface {
	Create(ctx context.Context, m *Calibration) error
	List(ctx context.Context, opts gormx.ListOptions) ([]Calibration, error)
	Count(ctx context.Context, opts gormx.ListOptions) (int64, error)
	Update(ctx context.Context, m *Calibration) error
	GetByID(ctx context.Context, id string) (*Calibration, error)
	DeleteByID(ctx context.Context, id string) error
	GetByName(ctx context.Context, v string) (*Calibration, error)
}

// CalibrationStore provides typed CRUD access to Calibration records.
// Calibration opts out of instrumentation entirely, proving a table-level (telemetry.v1.telemetry).enabled=false override wins over the tree-wide default.
type CalibrationStore struct {
	DB *gorm.DB
	// Telemetry observes every operation; nil is a no-op. Wire the generated
	// adapter: NewCalibrationStore(db).WithTelemetry(telemetry.New(o)).
	Telemetry gormx.Telemetry
}

// Compile-time proof that CalibrationStore implements its own interface. Without it a
// signature could drift from CalibrationStoreIface and only break in whichever downstream
// package decorates it.
var _ CalibrationStoreIface = (*CalibrationStore)(nil)

// Compile-time proof that CalibrationStore satisfies the generic gormx.Store, so the
// generic engine can drive it alongside the typed finders below.
var _ gormx.Store[Calibration] = (*CalibrationStore)(nil)

// NewCalibrationStore returns a CalibrationStore backed by db.
func NewCalibrationStore(db *gorm.DB) *CalibrationStore { return &CalibrationStore{DB: db} }

// WithTelemetry sets the store's Telemetry and returns the store for chaining.
func (s *CalibrationStore) WithTelemetry(t gormx.Telemetry) *CalibrationStore {
	s.Telemetry = t
	return s
}

// Create inserts m.
func (s *CalibrationStore) Create(ctx context.Context, m *Calibration) error {
	return s.DB.WithContext(ctx).Create(m).Error
}

// List returns the Calibration records matching opts.
func (s *CalibrationStore) List(ctx context.Context, opts gormx.ListOptions) ([]Calibration, error) {
	var out []Calibration
	if err := opts.Apply(s.DB.WithContext(ctx)).Find(&out).Error; err != nil {
		return nil, err
	}
	return out, nil
}

// Count returns the number of Calibration records matching opts.Where
// (pagination and ordering are ignored).
func (s *CalibrationStore) Count(ctx context.Context, opts gormx.ListOptions) (int64, error) {
	var n int64
	db := s.DB.WithContext(ctx).Model(&Calibration{})
	if opts.Where != nil {
		db = db.Where(opts.Where, opts.Args...)
	}
	if err := db.Count(&n).Error; err != nil {
		return 0, err
	}
	return n, nil
}

// Update persists every field of m, which must carry its primary key.
func (s *CalibrationStore) Update(ctx context.Context, m *Calibration) error {
	return s.DB.WithContext(ctx).Save(m).Error
}

// GetByID fetches the Calibration with the given primary key.
func (s *CalibrationStore) GetByID(ctx context.Context, id string) (*Calibration, error) {
	var m Calibration
	if err := s.DB.WithContext(ctx).First(&m, "id = ?", id).Error; err != nil {
		return nil, err
	}
	return &m, nil
}

// DeleteByID removes the Calibration with the given primary key.
func (s *CalibrationStore) DeleteByID(ctx context.Context, id string) error {
	return s.DB.WithContext(ctx).Delete(&Calibration{}, "id = ?", id).Error
}

// GetByName fetches the Calibration with the given name (a unique column).
func (s *CalibrationStore) GetByName(ctx context.Context, v string) (*Calibration, error) {
	var m Calibration
	if err := s.DB.WithContext(ctx).First(&m, "name = ?", v).Error; err != nil {
		return nil, err
	}
	return &m, nil
}
