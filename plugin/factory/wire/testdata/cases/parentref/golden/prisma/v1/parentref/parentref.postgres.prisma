// Code generated by protoc-gen-store. DO NOT EDIT.
// versions:
// 	protoc-gen-store (unknown)
// 	protoc (unknown)
// source: parentref.proto
//
// database: v1
// schemas:  parentref_v1
//
// annotations: entity.v1 (unknown), store.v1 (unknown)
// engine:      protokit (unknown)
//
// store — https://github.com/the-protobuf-project/store

/// User is the parent resource.
model User {
  /// Unique identifier for the record.
  id String @id @default(ulid()) @map("id")

  /// Resource name; the AIP identifier.
  name String @unique @map("name")

  /// display_name is shown in UIs.
  displayName String @map("display_name")

  /// Back-relation: Note records that reference this model via user_id.
  notes Note[]

  /// Maps the model to the "users" table in the database.
  @@map("users")
  /// Maps the model to the "parentref_v1" schema in the database.
  @@schema("parentref_v1")
}

/// Note is owned by a User. Its pattern carries a {user} parent segment with no corresponding field, so store materializes a user_id FK → User from the pattern alone.
model Note {
  /// Unique identifier for the record.
  id String @id @default(ulid()) @map("id")

  /// Resource name; the AIP identifier.
  name String @unique @map("name")

  /// body is the note's text.
  body String @map("body")

  /// Parent reference to User (from the AIP resource pattern).
  userID String @map("user_id")

  /// Relation to User via user_id.
  user User @relation(fields: [userID], references: [id], onDelete: Cascade)

  /// Index on [user_id].
  @@index([userID])
  /// Maps the model to the "notes" table in the database.
  @@map("notes")
  /// Maps the model to the "parentref_v1" schema in the database.
  @@schema("parentref_v1")
}
