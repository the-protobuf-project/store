# Connection URL for the shop_db database, pre-populated from the proto
# datasource url (regenerated with the schema; git-ignored). Edit locally if
# your credentials differ. shop_db.config.ts reads it via env("SHOP_DB_DATABASE_URL").
SHOP_DB_DATABASE_URL="postgresql://user:password@localhost:5432/shop_db"
