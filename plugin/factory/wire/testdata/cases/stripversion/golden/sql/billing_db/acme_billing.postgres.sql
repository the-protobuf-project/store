-- Code generated by protoc-gen-store. DO NOT EDIT.
-- versions:
-- 	protoc-gen-store (unknown)
-- 	protoc (unknown)
-- source: billing.proto
--
-- database: billing_db
-- schema:   acme_billing
--
-- annotations: entity.v1 (unknown), store.v1 (unknown)
-- engine:      protokit (unknown)
--
-- store — https://github.com/the-protobuf-project/store

CREATE SCHEMA IF NOT EXISTS "acme_billing";

-- Invoice lands in schema "acme_billing", not "acme_billing_v1": the store.yaml strip_version option flattens the trailing API version out of the resource-type-derived schema name.
CREATE TABLE "acme_billing"."invoices" (
    -- Unique identifier for the record.
    "id"  CHAR(26)  NOT NULL  PRIMARY KEY,
    -- Resource name; the AIP identifier.
    "name"  VARCHAR(255)  NOT NULL  UNIQUE,
    -- Amount due, in minor units.
    "amount"  VARCHAR(255)  NOT NULL
);


-- Column and table documentation, persisted to the catalog.
COMMENT ON TABLE "acme_billing"."invoices" IS 'Invoice lands in schema "acme_billing", not "acme_billing_v1": the store.yaml strip_version option flattens the trailing API version out of the resource-type-derived schema name.';
COMMENT ON COLUMN "acme_billing"."invoices"."id" IS 'Unique identifier for the record.';
COMMENT ON COLUMN "acme_billing"."invoices"."name" IS 'Resource name; the AIP identifier.';
COMMENT ON COLUMN "acme_billing"."invoices"."amount" IS 'Amount due, in minor units.';
