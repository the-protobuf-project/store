// Code generated by protoc-gen-store. DO NOT EDIT.
// versions:
// 	protoc-gen-store dev
// 	protoc (unknown)
// source: bookstore/v1/inventory.proto
//
// database: bookstore_db
// schemas:  inventory
//
// annotations: entity.v1 dev, store.v1 dev
// engine:      protokit v1.2.1
//
// store — https://github.com/the-protobuf-project/store

/// Shelf groups books physically. The resource's `plural` fixes the irregular plural ("shelfs" → "shelves") — no table name override needed.
model Shelf {
  /// Unique identifier for the record.
  id String @id @default(ulid()) @map("id")

  /// name: IDENTIFIER → PRIMARY KEY, VARCHAR(255).
  name String @unique @map("name")

  /// theme: REQUIRED → NOT NULL VARCHAR(255).
  theme String @map("theme")

  /// capacity is the number of books the shelf holds; nullable INTEGER.
  capacity Int? @map("capacity")

  /// Timestamp when the record was created.
  createdAt DateTime @default(now()) @bookstore_db.Timestamptz(6) @map("created_at")

  /// Timestamp when the record was last updated.
  updatedAt DateTime @updatedAt @bookstore_db.Timestamptz(6) @map("updated_at")

  /// Maps the model to the "shelves" table in the database.
  @@map("shelves")
  /// Maps the model to the "inventory" schema in the database.
  @@schema("inventory")
}
