go 1.26.4

// Root module — plugin binary, the entity.v1 vocabulary under plugin/entity/,
// and generated pb stubs all live here.
use .

// Examples are a separate module so their transitive deps (generated gRPC
// stubs, etc.) don't pollute the plugin's dependency graph.
use ./examples
